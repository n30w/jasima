syntax = "proto3";

package chat;

option go_package = "./chat";

message Message {
  string sender = 1;
  string receiver = 2;
  string content = 3;
}

service ChatService {
  rpc Chat(stream Message) returns (stream Message) {}
}
